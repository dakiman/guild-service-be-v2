server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php;

    # Docker's embedded DNS + a variable upstream force per-request resolution,
    # so recreating the app container doesn't require restarting this nginx
    # (a static name is resolved once at startup and 502s after recreate).
    resolver 127.0.0.11 valid=10s ipv6=off;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    client_max_body_size 10M;

    gzip on;
    gzip_types application/json text/plain application/xml;
    gzip_min_length 256;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        set $fpm_upstream guild-service-v2-app;
        fastcgi_pass $fpm_upstream:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_buffering on;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 16 16k;
        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 30s;
    }

    location ~ /\. {
        deny all;
    }

    location = /health-nginx {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    access_log /dev/stdout;
    error_log /dev/stderr warn;
}
